<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- opens a subscreen of settings -->

    <!-- default value is a boolean -->
    <PreferenceScreen
        android:key="button_voicemail_category_key"
        android:persistent="false"
        android:title="Voicemail">

        <ListPreference
            android:key="button_voicemail_provider_key"
            android:title="Voicemal Provider" />

        <!-- opens another nested subscreen -->
        <PreferenceScreen
            android:key="button_voicemail_setting_key"
            android:persistent="false"
            android:title="Voicemail Settings"></PreferenceScreen>

        <RingtonePreference
            android:key="button_voicemail_ringtone_key"
            android:ringtoneType="notification"
            android:title="Voicemail Ringtone Title" />
    </PreferenceScreen>
    <CheckBoxPreference
        android:title="Checkbox"
        android:defaultValue="true" />

    <!-- default value is a string -->
    <SwitchPreference
        android:defaultValue="false"
        android:key="switch_preference_1"
        android:title="Switch preference" />
    <SwitchPreference
        android:defaultValue="false"
        android:key="switch_preference_2"
        android:title="Switch preference" />
    <SwitchPreference
        android:defaultValue="false"
        android:key="switch_preference_3"
        android:title="Switch preference" />

</PreferenceScreen>